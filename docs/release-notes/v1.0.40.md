## Highlights

- The strict JSDoc type-checking gate expanded to the entire asset parser layer (9 modules, including the glossary/TBX/TMX parsers and the TB structure detector), and to the provider integration core: transport support, prompt builder, and provider registry (approximately 1,900 additional lines now verified by `tsc --noEmit`).
- The preview SOAP payload parsers, preview state support, rule engine, snapshot settle helper, and prompt preset store received full annotations, closing the type-safety gap around memoQ preview data shapes.
- The provider capability lookup tables, rate-limit hint parsing, and response error mapping now carry explicit shapes, making the OpenAI-compatible request surface contract-checked at the type level.

## Compatibility

- No IPC channel, HTTP route, database schema, plugin protocol, Provider, Profile, asset, QA, history, or update-manifest behavior changed. The database schema version stays at 1.
- All changes are type-level annotations or behavior-equivalent rewrites covered by the existing test suite; no plugin-side change is required relative to v1.0.39.

## Verification

- `pnpm run typecheck` (strict `checkJs`) passed with zero errors across the expanded typed surface (shared, QA, bilingual, asset, provider config/governance/response/transport/prompt-builder/registry, gateway validation, renderer IPC surface).
- Desktop regression passed 616 tests: 612 passed with 4 packaging-only conditional skips and no failures.
- Repository governance passed 28 tests; ESLint passed with zero errors and zero warnings.
- Native plugin Release build completed with 0 warnings and 0 errors, and the plugin regression suite passed.
- Windows release packaging completed end to end. The final locally packaged 134,063,176-byte ZIP and 96,899,780-byte 7z independently matched the generated stable manifest: ZIP `1ca719aba9ca26f6c867c7406c6f621b5fab7a839b51d467d049e2bba6d375c5`; 7z `3b4fa7955eb1cdfbca8cbd1639dbc70338d04cdf232ef15fd530a07a581be234`.

## Rollback watchpoints

- Roll back to `v1.0.39` if the expanded type gate blocks a legitimate hotfix; prefer extending the annotations over weakening `strict` or shrinking the tsconfig include list.
- The change set is type-level and internal to the desktop app; no data or protocol migration is involved.
