## Highlights

- Updated the default provider model to `gpt-5.4-mini` for new provider setups.
- Reduced manual setup steps when creating a new OpenAI or OpenAI-compatible provider.

## Upgrade Notes

- Existing saved providers are not rewritten automatically.
- The new default applies when creating a provider without explicitly choosing a model.
