## Fixes

- Fixed the startup display error `Cannot access ... before initialization` by initializing the provider controller before the history filters that consume its state setter.
- Restored missing provider draft helpers, provider/profile draft references, responsive navigation state, and unsaved-change tracking after the renderer controller extraction.
- Reconnected asset preview and return-to-history actions, and removed obsolete history-detail setters so closing the detail view uses the owning lifecycle hook.

## Regression protection

- Added a minified Vite build and first-render regression test that reproduces the startup failure before the fix.
- Enabled renderer checks for undefined variables and use before initialization.

## Compatibility

- No database schema, IPC contract, provider credentials, or memoQ plugin protocol changes are required.
- Close the previous app and extract the new portable package into a new directory. Keep existing user data and configuration.
