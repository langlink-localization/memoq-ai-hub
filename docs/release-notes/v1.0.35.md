## Highlights

- Extracted renderer refresh, visibility-aware polling, history-detail loading, shell persistence, scroll restoration, resize handling, and unsaved-draft protection from the application shell into focused lifecycle hooks.
- Reused stable latest-callback and polling boundaries across Quality Check, Preview Assistant, QA history, prompt presets, and finding-feedback loading.
- Reduced `App.jsx` by 134 lines while preserving its role as the renderer orchestration shell.
- Resolved all 20 existing React Hooks dependency warnings and changed `pnpm run lint` to fail on any warning.
- Added focused coverage for hidden-window polling, timer cleanup, history-detail success, bridge failure, and late responses after cancellation.

## Compatibility

- No IPC, HTTP route, database schema, plugin protocol, Provider, Profile, QA, history, asset, or update-manifest contract changed.
- Refresh intervals and page-owned data-loading conditions remain unchanged.
- memoQ 12 live-host validation remains pending. This release does not claim that Confidence or Info display has been verified in a real memoQ host.

## Verification

- ESLint passed with zero errors and zero warnings under the new fail-on-warning gate.
- Focused renderer lifecycle and shell contracts passed 47 tests.
- Desktop regression passed 583 tests: 579 passed with 4 packaging-only conditional skips and no failures.
- Repository governance passed 26 tests; the Ant Design full scan passed with 0 findings and no skipped files.
- Native plugin Release build completed with 0 warnings and 0 errors, and plugin regression passed.
- Windows release packaging completed end to end, and all 4 package-state checks passed against the final bundle.
- Independently calculated SHA-256 values matched the stable manifest for the 134,051,281-byte ZIP and 96,892,844-byte 7z artifacts.

## Rollback watchpoints

- Roll back to `v1.0.34` if initial state loading, dashboard/startup polling, Quality polling, history detail, page scroll restoration, or unsaved-draft protection regresses.
- A rollback restores warning-tolerant lint. Do not suppress new Hooks warnings as a workaround; fix the dependency or lifecycle boundary before the next release.
