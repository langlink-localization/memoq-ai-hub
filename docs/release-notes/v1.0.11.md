# memoQ AI Hub v1.0.11

## What's New

- Fixed a first-run crash on the `Providers` page when the desktop app starts with no configured providers yet.
- Opening `Providers` from a fresh portable unzip now shows the expected empty state instead of failing during renderer startup.

## Validation

- Focused provider connection regression tests passed with `node --test apps/desktop/test/providerConnectionUx.test.js`.

## Notes

- This release is a targeted stability fix for fresh installs and first-run setup.
